# AGENTS.md — 新项目协作基线

本仓库当前是一个干净的新项目工作区。旧 ObjGauss 项目已归档到 Git 标签
`archive/objgauss-final-2026-07-14`；旧项目规则只属于该归档，不适用于新项目。

## 开工协议

- 开始工作前先阅读 `README.md` 和 `REFERENCES.md`，再运行 `git status --short`。
- 工作区中的既有改动默认属于 Owner 或其他工作流；不覆盖、回退、暂存或整理无关改动。
- 先用 `rg` / `rg --files` 定位真实入口、调用方、测试和局部 `AGENTS.md`。
- 新项目方向、技术栈、目录边界和验收方式尚未确定时，不预建框架、服务或抽象。

## 修改纪律

- 每次只完成一个可观察目标，保持 diff 小而内聚，优先修复根因。
- 修改前简述目标文件、目标、范围外事项和验证方式。
- 行为变化应有行为级测试；无法自动化时提供可复现验证并说明残余风险。
- 不为通过检查而删除测试、放宽断言、吞掉异常或伪造结果。
- 不提交依赖目录、构建产物、缓存、下载数据、训练数据、checkpoint、凭据或未脱敏日志。
- 新增生产依赖、外部服务、持久化、公共 contract 或不可逆迁移前，先取得 Owner 确认。

## Git 与外部操作

- 未经 Owner 动作级明确授权，不执行 commit、amend、rebase、push、merge、tag、发布或部署。
- 禁止强制推送、`git reset --hard` 和 `git clean -fd`，除非 Owner 明确要求且风险已确认。
- 一个提交只表达一个意图，精确暂存相关文件，不使用 `git add .`。
- 不改写或删除 `archive/objgauss-final-2026-07-14`；它是旧项目的恢复点。

## 完成与沟通

- 默认使用中文沟通；代码、命令、路径和标识符保持原样。
- 最终回复先给结果，再说明关键变更、实际验证、未验证项和残余风险。
- 只声明真实达到的完成层级；未运行的命令不得声称通过。
- 新项目稳定后，应把本文件补充为该项目真实、可执行、可验证的规则，避免复制临时状态。
